// Bundled gallery examples (curated). The data lives in galleryExamples.json so it
// only gets pulled into the gallery's lazy chunk — never the main editor bundle.
import examples from './galleryExamples.json'

export const GALLERY_EXAMPLES = examples

export default GALLERY_EXAMPLES
